import { submitResults } from '../../helpers';
import { BaseTest } from '../../types';
import { NightwatchBrowser, NightwatchCallbackResult } from 'nightwatch';


const {{properCase name}}Test: BaseTest = {
'@disabled': false,
tags: [],
desiredCapabilities: {
name: {{titleCase name}},
},


'step 1':(browser: NightwatchBrowser) => {},

after: (browser, done) => submitResults(browser, done),

}


export = {{properCase name}}Test;